export interface TftTrait {
    name: string,
    numUnits: number,
    style: number,
    tierCurrent: number,
    tierTotal: number
}

export interface TftTraitData {
    key: string
    name: string
}